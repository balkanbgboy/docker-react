language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t madolka/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true madolka/docker-react npm run test